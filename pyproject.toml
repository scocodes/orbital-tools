[project]
name = "orbital-tools"
version = "0.1.0"
description = "Reusable orbital dynamics, atmosphere and integration tools"
requires-python = ">=3.10"
dependencies = ["numpy"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"